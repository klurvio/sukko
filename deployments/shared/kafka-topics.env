# =============================================================================
# Kafka Topics Configuration
# =============================================================================
# Purpose: Single source of truth for Kafka topic configuration
# Used by: Publisher, WS Server, topic creation scripts

# =============================================================================
# TOPIC LIST
# =============================================================================
# Comma-separated list of topics (no spaces)
KAFKA_TOPICS=odin.trades,odin.liquidity,odin.balances,odin.metadata,odin.social,odin.community,odin.creation,odin.analytics

# =============================================================================
# TOPIC CONFIGURATION
# =============================================================================
# Partitions per topic (parallel processing)
KAFKA_PARTITIONS=12

# Replication factor (1 = no replication for local/single-broker)
KAFKA_REPLICAS=1

# =============================================================================
# RETENTION POLICIES (Per-Topic)
# =============================================================================
# Format: topic:retention_time
# Used by topic creation scripts
KAFKA_RETENTION_odin_trades=30s
KAFKA_RETENTION_odin_liquidity=60s
KAFKA_RETENTION_odin_metadata=1h
KAFKA_RETENTION_odin_social=1h
KAFKA_RETENTION_odin_community=5m
KAFKA_RETENTION_odin_creation=1h
KAFKA_RETENTION_odin_analytics=5m
KAFKA_RETENTION_odin_balances=30s
