# Prometheus Configuration - Isolated Setup
# Scrapes metrics from remote instances via internal network
#
# This file is templated. Before deployment, substitute:
#   ${WS_GO_INTERNAL_IP} - Internal IP of ws-go instance
#   ${VALKEY_INTERNAL_IP} - Internal IP of Valkey instance
#
# Example:
#   export WS_GO_INTERNAL_IP=10.128.0.3
#   export VALKEY_INTERNAL_IP=10.128.0.10
#   envsubst < prometheus.yml > prometheus.deployed.yml

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # WebSocket server (remote instance)
  - job_name: "ws-go"
    static_configs:
      - targets: ["${WS_GO_INTERNAL_IP}:3004"]
        labels:
          instance: "odin-ws-go"
          service: "websocket"

  # Redpanda metrics
  - job_name: "redpanda"
    static_configs:
      - targets: ["redpanda:9644"]
        labels:
          service: "kafka"

  # Publisher metrics
  - job_name: "publisher"
    static_configs:
      - targets: ["publisher:3003"]
        labels:
          service: "publisher"

  # Prometheus self-monitoring
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Valkey Exporter (BroadcastBus infrastructure)
  - job_name: "valkey"
    static_configs:
      - targets: ["${VALKEY_INTERNAL_IP}:9121"]
        labels:
          instance: "ws-valkey"
          service: "valkey"
          role: "broadcast-bus"
