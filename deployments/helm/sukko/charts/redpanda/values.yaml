# Default values for redpanda subchart

enabled: true

replicaCount: 1

image:
  repository: redpandadata/redpanda
  tag: v23.3.5
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: "500m"
    memory: "512Mi"
  limits:
    cpu: "1"
    memory: "1Gi"

# Ports
kafkaPort: 9092
adminPort: 9644
pandaproxyPort: 8082
schemaRegistryPort: 8081
consolePort: 8080

# Storage
storage:
  enabled: true
  size: 5Gi
  storageClass: ""

# External access for sukko-api publisher
# When enabled with advertisedHost, Redpanda uses dual listeners:
# - internal://sukko-redpanda:9092 for in-cluster clients (ws-server)
# - external://<advertisedHost>:<kafkaPort> for external clients (publisher VM)
externalAccess:
  enabled: false
  type: LoadBalancer  # LoadBalancer or NodePort
  nodePort: ""        # Only used if type is NodePort
  advertisedHost: ""  # External IP or hostname (required for external producers)
  loadBalancerIP: ""  # Static IP for LoadBalancer (from Terraform google_compute_address)
  annotations: {}     # e.g. {"cloud.google.com/load-balancer-type": "Internal"}
  # External listener port inside container (maps to kafkaPort on LoadBalancer)
  containerPort: 19092

# Topic configuration (matches v1 deployment)
topics:
  - name: sukko.trades
    partitions: 12
    replicationFactor: 1
  - name: sukko.liquidity
    partitions: 12
    replicationFactor: 1
  - name: sukko.balances
    partitions: 12
    replicationFactor: 1
  - name: sukko.metadata
    partitions: 12
    replicationFactor: 1
  - name: sukko.social
    partitions: 12
    replicationFactor: 1
  - name: sukko.community
    partitions: 12
    replicationFactor: 1
  - name: sukko.creation
    partitions: 12
    replicationFactor: 1
  - name: sukko.analytics
    partitions: 12
    replicationFactor: 1

# Pod Security Context
# Redpanda image runs as "redpanda" user (UID 101)
# fsGroup ensures PVC is accessible by the redpanda user
podSecurityContext:
  fsGroup: 101
  runAsUser: 101
  runAsGroup: 101

podAnnotations: {}

nodeSelector: {}

tolerations: []

affinity: {}
