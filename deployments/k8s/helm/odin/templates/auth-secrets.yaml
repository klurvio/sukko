# Shared authentication secrets for auth-service and ws-server
# Contains JWT_SECRET and app secrets
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth-secrets
  labels:
    app.kubernetes.io/part-of: odin
    helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version | replace "+" "_" | trunc 63 | trimSuffix "-" }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
data:
  # JWT signing secret (base64 encoded)
  # Generate with: openssl rand -base64 32 | base64
  jwt-secret: {{ .Values.auth.jwtSecret | b64enc | quote }}
  {{- range $key, $value := .Values.auth.appSecrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
