# Private keys must never be committed.
# Only the committed production/placeholder public key (sukko.pub) is embedded by
# default builds. The dev/e2e keypair (sukko.dev.key + sukko.dev.pub) is regenerated
# by genkeys and MUST NOT be committed — the sukko_e2e build embeds sukko.dev.pub.
*.key
sukko.dev.pub
