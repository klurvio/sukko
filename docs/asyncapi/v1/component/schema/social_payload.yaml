# Social Payload Schemas
# Data structures for odin.social events

SocialPayload:
  type: object
  description: Payload for social-related events
  oneOf:
    - $ref: '#/TwitterVerifiedPayload'
    - $ref: '#/SocialLinksUpdatedPayload'

TwitterVerifiedPayload:
  type: object
  title: Twitter Verified
  description: Twitter verification confirmed
  required:
    - twitterHandle
    - verified
    - verifiedAt
  properties:
    twitterHandle:
      type: string
      description: Twitter handle
      example: "@tokenBTC"
    verified:
      type: boolean
      description: Verification status
      example: true
    verifiedAt:
      type: integer
      format: int64
      description: Verification timestamp in milliseconds
      example: 1699901234567

SocialLinksUpdatedPayload:
  type: object
  title: Social Links Updated
  description: Social media links changed
  properties:
    twitter:
      type: string
      format: uri
      description: Twitter profile URL
      example: "https://twitter.com/tokenBTC"
    telegram:
      type: string
      format: uri
      description: Telegram group URL
      example: "https://t.me/tokenBTC"
    discord:
      type: string
      format: uri
      description: Discord server URL
      example: "https://discord.gg/tokenBTC"
    website:
      type: string
      format: uri
      description: Project website URL
      example: "https://tokenbtc.com"
